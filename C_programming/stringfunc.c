//5.wap to accept and display name using gets and puts function.
int main()
{
	char name[20];
	 
	printf("enter a name :");
	gets(name);//gets function is used to accept a string from the user(include space also).
	
	puts(name);
	
}
