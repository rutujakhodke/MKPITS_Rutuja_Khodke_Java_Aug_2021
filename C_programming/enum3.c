//3.create a enum account having members saving ,current and loan.
enum Accounttype{saving=1,current,loan};

int main()
{
	enum Accounttype act;
	act=current;
	printf(" your accounttype is =%d",act);
}
