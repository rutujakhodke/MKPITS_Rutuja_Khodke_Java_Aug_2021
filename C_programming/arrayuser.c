//2.wap to create an integer array of 3 elements to store 3 no. accept 3 no. from the user and store it in an array  
// then display the value stored in an array .
int main()
{
	int num[3]; //it will create an array of 3 elements
	//storing 3 no. in 3 elements of array
	printf("\nenter a number");
    	scanf("%d",&num[0]);
	printf("\nenter a number");
	    scanf("%d",&num[1]);
	printf("\nenter a number");
	    scanf("%d",&num[2]);
	    //display the values stored in an array
	    printf("\nnum[0]=%d",num[0]);
	    printf("\nnum[1]=%d",num[1]);
	    printf("\nnum[2]=%d",num[2]);
	    
}
