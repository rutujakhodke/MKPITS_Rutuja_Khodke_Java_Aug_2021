//3.wap to accept a number and print that no.
int main()
{
	int num;
	//scanf is a function to accept some input from the user.
	printf("enter a number :");
	scanf("%d",&num);//& is used to specify the address of num variable.
	printf("number =%d",num);
	
}
