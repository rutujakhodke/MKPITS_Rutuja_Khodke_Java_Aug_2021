//7.wap to accept length,breadth and height and then print volume.
int main()
{
	int length,breadth,height,vol;
	printf("enter a length,breadth and height of a cube :");
	scanf("%d%d%d",&length,&breadth,&height);
	vol=length*breadth*height;
	printf("volume of a cube =%d",vol);
	
}
